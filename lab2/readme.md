# File System (FS Module)

FS module directly communicates with Operating System rather than browser
The common operations on a file or folder are

1. file -> rightFile,readFile,appendFile
2. folder --> rmdir/rm,readdir,mkdir/md
3. filestat -->stat,lstat,rstat
4. watch -> watch , unwatch
5. stream ->readStream(),writeStream()
   all task
